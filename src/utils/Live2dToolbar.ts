import { ref, computed } from 'vue'
import { ChatService } from './ChatService'

export {
  contextMenuVisible,
  contextMenuStyle,
  contextMenuItems,
  showContextMenu,
  hideContextMenu,
  handleMouseDown,
}
